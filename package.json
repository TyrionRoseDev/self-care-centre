{
  "name": "self-care-centre",
  "version": "1.0.0",
  "description": "Self-Care Centre app + built-in web-push reminders (single deployment)",
  "type": "commonjs",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "build": "node build/build.js",
    "vapid": "node -e \"const w=require('web-push');const k=w.generateVAPIDKeys();console.log('VAPID_PUBLIC='+k.publicKey);console.log('VAPID_PRIVATE='+k.privateKey);\""
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-cron": "^3.0.3",
    "web-push": "^3.6.7"
  }
}
